<div>
  <h1><%= t('.header') %></h1>
    <table class="table">
        <tr>
          <th scope="col"><%= t('.name') %></th>
          <th scope="col"><%= t('.picture') %></th>
          <th scope="col"><%= t('.given_for') %></th>
          <th scope="col"><%= t('.action') %></th>
        </tr>
      <%= render @badges %>
    </table>
  </div>
</div>

<%= link_to 'Создать награду', new_admin_badge_path %>
