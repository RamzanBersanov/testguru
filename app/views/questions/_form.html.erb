<%= form_with model: [@test, @question], url: test_questions_path(@test, @question) do |form| %>
  <%= render 'shared/errors', resource: @test %>
  <%= form.text_field :body %>

  <%= form.submit %>
<% end %>

<%= render 'back' %>
