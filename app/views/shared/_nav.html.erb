<nav class="navbar navbar-light bg-light">
  <%= link_to t('.testguru'), tests_path, class: 'navbar-brand' %>
  <span class="navbar-text">
    <% if user_signed_in? && current_user.type == 'Admin' %>
      <%= link_to t('.admin_panel'), admin_tests_path %>
    <% end %>
  </span class="navbar-text">
  <span class="navbar-text">
    <% if user_signed_in? %>
      <b class="nav user-email"><%= t('.greeting', current_user_name: current_user.name) %></b> |
      <%= link_to t('.logout'), destroy_user_session_path,  method: :delete %>
    <% end %>
  </span>
</nav>
